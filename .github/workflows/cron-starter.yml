name: 'Weekly Workflow'

on: workflow_dispatch

jobs:
  Upload_Job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: Checkout repo and run
        with:
          persist-credentials: 'false'
        run: |
          npm install
          ./triggerRun.sh ${{ vars.PLAYLIST_ONE }}